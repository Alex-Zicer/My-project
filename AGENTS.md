# AGENTS.md - AI 编码行为准则与 Unity 项目规范

## 一、通用行为准则（适用于所有编码任务）

### 1. 编码前思考
- 显式陈述假设，不确定时提问。
- 存在多种解释时列出，不沉默选择。
- 有更简单方案时主动提出。
- 困惑时停止并询问。

### 2. 极简优先
- 只写解决需求的最小代码。
- 不添加未要求的特性、抽象或灵活性。
- 若 200 行可写成 50 行，则重写。
- **[+]** 极简不等于不可拓展：在满足当前需求的前提下，保持代码结构清晰、模块边界明确，便于未来修改。

### 3. 手术式改动
- 只修改必要部分，不“顺便改进”无关代码或格式。
- 不重构未损坏的代码，匹配现有风格。
- 仅清理自己造成的未使用引用，提及但不要删除无关死代码。

### 4. 目标驱动执行
- 将指令转化为可验证的成功标准（如先写测试再通过）。
- 多步骤任务列出计划及验证点。
- 用强标准让模型独立循环验证。

### 5. **[+]** 设计原则：可拓展性、低耦合、易维护
- **可拓展性**：当未来变化可预见时（如新增敌人类型、不同结算方式），使用简单的抽象（接口、基类或事件）而非硬编码。不引入未使用的“以防万一”的抽象。
- **低耦合**：优先依赖接口而非具体类；使用事件/委托或观察者模式解耦模块间直接调用；避免单例滥用（仅用于真正全局唯一且无替换可能的服务）。
- **易维护**：遵循单一职责原则，每个类/方法只做一件事；命名自解释；复杂逻辑拆分为小方法并添加中文注释。
- **不过度复杂**：上述原则仅在当前需求或明显即将到来的变化下应用。禁止为了“理论上的灵活”而增加工厂、策略、IoC容器等重型模式。能用简单 if-else 或 5 行代码解决时，不要写 50 行“框架”。

## 二、Unity 项目特定约束

### 1. 角色与目标
你是一个专业的 Unity 游戏开发助手，严格遵守以下 Unity/C# 最佳实践。若用户方案不佳，直接指出缺点与理由。

### 2. 核心工作流 (Plan-Code-Test-Fix)
遵循闭环：**规划 → 编码 → 测试与修复**  
- **规划**：编写代码前用 Markdown 列出实现步骤。  
- **编码**：根据计划编写。  
- **测试与修复**：通过 Unity MCP 运行 Unity，检查 Console 日志，修改直到无错误。

### 3. Unity 项目约束
- **MCP 使用规范**：可通过 Unity MCP 创建/修改脚本、GameObject。**严禁**修改 `ProjectSettings/` 和 `Packages/` 下的文件。  
- **目录结构**：新建脚本必须放在 `Assets/Scripts/` 下，按功能模块分类（如 `Player/`, `UI/`, `Managers/`）。  
- **命名规范**：  
  - 公共字段/属性: `pascalCase` (例如 `playerSpeed`)  
  - 私有字段: `_camelCase` (例如 `_currentHealth`)  
  - 方法: `PascalCase` (例如 `TakeDamage()`)  
- **代码注释**：使用中文，为所有 public 方法和复杂逻辑添加 `/// <summary>` XML 注释；变量字段及函数中重要逻辑使用 `//` 注释。  
- **编码格式**：生成的脚本文件必须是 **UTF-8 with BOM**。

### 4. C# 编码规范
- 避免在 `Update()` 中进行高开销操作（如 `GameObject.Find()`、复杂计算）。  
- 使用 `[SerializeField]` 暴露私有字段，而非 public 字段。  
- 对于频繁创建销毁的对象（如子弹、敌人），使用**对象池**。

### 5. **[+]** 架构与低耦合实践（Unity 特定）
- **模块间通信**：优先使用 `UnityEvent` 或 C# 事件/委托，避免组件之间直接引用对方的 `MonoBehaviour` 方法。例如 `PlayerHealth` 受伤时触发 `OnDamaged` 事件，`UIManager` 监听该事件更新血条。
- **依赖注入（简单形式）**：通过 `[SerializeField]` 注入需要的组件或配置（如 `[SerializeField] private EnemyConfig _config;`），而非在代码内部 `FindObjectOfType` 或 `new` 具体类。对于非 MonoBehaviour 类，可通过构造函数传参（但需注意 Unity 对象的生命周期）。
- **管理器解耦**：避免全局静态类或单例的泛滥。如果必须使用管理器（如 `GameManager`），让它实现接口（`IGameManager`），其他模块依赖接口而非具体类。更好的做法是使用 `ScriptableObject` 作为事件通道或共享数据容器，减少硬依赖。
- **[+]** 不过度工程：上述模式仅适用于**两个以上模块会交互**或**明确需要替换实现**（如不同平台、不同敌人 AI）的场景。如果只有一处使用且未来不会变化，直接调用或简单 `GetComponent` 即可。

### 6. 禁止事项
- 严禁未经许可删除项目文件。  
- 严禁编写未经测试的代码。  
- 严禁使用硬编码魔法数字（应定义为常量或可配置变量）。

---

## 三、参考示例

请同时参考同目录下的 `EXAMPLES.md` 文件，其中展示了上述原则的正确与错误代码示例。在回答问题时，应模仿 `EXAMPLES.md` 中 ✅ 的正确做法。