// Resolved phase of one dialogue route.
public enum DialogueRoutePhase
{
    // First-time dialogue from a rule.
    First,

    // Repeat dialogue from a rule.
    Repeat,

    // Default profile dialogue when no rule matches.
    Default
}
